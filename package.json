{
  "name": "playground-helm-crud",
  "version": "1.0.0",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "e2e:test": "cd task-manager && bru run --reporter-junit ../results.xml  --reporter-html ../results.html --env default"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@usebruno/cli": "2.14.2"
  }
}
