apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "task-manager.fullname" . }}-test-connection"
  labels:
    {{- include "task-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: my-api-tests
      image: task-manager-e2e:latest
      imagePullPolicy: Never
      command: ["/bin/sh", "-c"]
      args:
        - |
          # By pass DNS resolution issue on Rancher desktop by using Kubernetes service environment variables
          export TARGET_HOST=$MANAGER_DEPLOY_TASK_MANAGER_SERVICE_HOST
          export TARGET_PORT=$MANAGER_DEPLOY_TASK_MANAGER_SERVICE_PORT
          
          export FULL_URL="http://$TARGET_HOST:$TARGET_PORT"
          # Run Bruno using the constructed URL
          bru run --env default --env-var base_url=$FULL_URL
  restartPolicy: Never