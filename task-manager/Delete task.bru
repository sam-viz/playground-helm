meta {
  name: Delete task
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/tasks/{{taskId}}
  body: none
  auth: inherit
}

tests {
  test("should delete a task", function () {
    expect(res.getStatus()).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
